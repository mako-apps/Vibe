import UIKit
let config = UIImage.SymbolConfiguration(paletteColors: [.red, .blue])
// Actually we can't easily test visual output in a CLI script unless we render it to an image and inspect pixels, which is overkill.
print("Swift script ready")
